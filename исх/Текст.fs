пространство Библиотека

модуль Текст =
   пусть Добавить(a:string,b:obj) = a + b.ToString()
   пусть ПолучитьДлину (текст:string) = текст.Length
   пусть ПолучитьПодТекст (текст:string,стартовыйИндекс,длина) = текст.Substring(стартовыйИндекс,длина)

